use package.builtin;

load("array.rkt");

use package.utils;
use package.array_;

def makeArray(size) = such([]): ~(array) {
  length(array) = size;
}

def push(elt, array) = such(array): ~(it) {
  def lastIdx = length(it);
  length(it) = lastIdx + 1;
  it[lastIdx] = elt;
}